function folderStats=calcPostAnalysisStatsKinetics(folderStats,measure)
    folderStats.(measure).stats.max(1,:)=mean(folderStats.(measure).max);
    folderStats.(measure).stats.max(2,:)=median(folderStats.(measure).max);
    folderStats.(measure).stats.max(3,:)=std(folderStats.(measure).max,0);
    folderStats.(measure).stats.mae(1,:)=mean(folderStats.(measure).mae);
    folderStats.(measure).stats.mae(2,:)=median(folderStats.(measure).mae);
    folderStats.(measure).stats.mae(3,:)=std(folderStats.(measure).mae,0);
    folderStats.(measure).stats.mean(1,:)=mean(folderStats.(measure).mean);
    folderStats.(measure).stats.mean(2,:)=median(folderStats.(measure).mean);
    folderStats.(measure).stats.mean(3,:)=std(folderStats.(measure).mean,0);
    folderStats.(measure).stats.median(1,:)=mean(folderStats.(measure).median);
    folderStats.(measure).stats.median(2,:)=median(folderStats.(measure).median);
    folderStats.(measure).stats.median(3,:)=std(folderStats.(measure).median,0);
    folderStats.(measure).stats.rms(1,:)=mean(folderStats.(measure).rms);
    folderStats.(measure).stats.rms(2,:)=median(folderStats.(measure).rms);
    folderStats.(measure).stats.rms(3,:)=std(folderStats.(measure).rms,0);
    folderStats.(measure).stats.std(1,:)=mean(folderStats.(measure).std);
    folderStats.(measure).stats.std(2,:)=median(folderStats.(measure).std);
    folderStats.(measure).stats.mad(1,:)=mean(folderStats.(measure).mad);
    folderStats.(measure).stats.mad(2,:)=median(folderStats.(measure).mad);
    folderStats.(measure).stats.mad(3,:)=std(folderStats.(measure).mad,0);
    folderStats.(measure).stats.topMax(1,:)=mean(folderStats.(measure).topMax,1);
    folderStats.(measure).stats.topMax(2,:)=median(folderStats.(measure).topMax,1);
    folderStats.(measure).stats.topMax(3,:)=std(folderStats.(measure).topMax,0,1);
    
    folderStats.(measure).percentage.stats.max(1,:)=mean(folderStats.(measure).percentage.max);
    folderStats.(measure).percentage.stats.max(2,:)=median(folderStats.(measure).percentage.max);
    folderStats.(measure).percentage.stats.max(3,:)=std(folderStats.(measure).percentage.max,0);
    folderStats.(measure).percentage.stats.mae(1,:)=mean(folderStats.(measure).percentage.mae);
    folderStats.(measure).percentage.stats.mae(2,:)=median(folderStats.(measure).percentage.mae);
    folderStats.(measure).percentage.stats.mae(3,:)=std(folderStats.(measure).percentage.mae,0);
    folderStats.(measure).percentage.stats.mean(1,:)=mean(folderStats.(measure).percentage.mean);
    folderStats.(measure).percentage.stats.mean(2,:)=median(folderStats.(measure).percentage.mean);
    folderStats.(measure).percentage.stats.mean(3,:)=std(folderStats.(measure).percentage.mean,0);
    folderStats.(measure).percentage.stats.median(1,:)=mean(folderStats.(measure).percentage.median);
    folderStats.(measure).percentage.stats.median(2,:)=median(folderStats.(measure).percentage.median);
    folderStats.(measure).percentage.stats.median(3,:)=std(folderStats.(measure).percentage.median,0);
    folderStats.(measure).percentage.stats.rms(1,:)=mean(folderStats.(measure).percentage.rms);
    folderStats.(measure).percentage.stats.rms(2,:)=median(folderStats.(measure).percentage.rms);
    folderStats.(measure).percentage.stats.rms(3,:)=std(folderStats.(measure).percentage.rms,0);
    folderStats.(measure).percentage.stats.std(1,:)=mean(folderStats.(measure).percentage.std);
    folderStats.(measure).percentage.stats.std(2,:)=median(folderStats.(measure).percentage.std);
    folderStats.(measure).percentage.stats.mad(1,:)=mean(folderStats.(measure).percentage.mad);
    folderStats.(measure).percentage.stats.mad(2,:)=median(folderStats.(measure).percentage.mad);
    folderStats.(measure).percentage.stats.topMax(1,:)=mean(folderStats.(measure).percentage.topMax,1);
    folderStats.(measure).percentage.stats.topMax(2,:)=median(folderStats.(measure).percentage.topMax,1);
    folderStats.(measure).percentage.stats.topMax(3,:)=std(folderStats.(measure).percentage.topMax,0,1);
    
    folderStats.(measure).stats.span(1,:)=mean(folderStats.(measure).span);
    folderStats.(measure).stats.span(2,:)=median(folderStats.(measure).span);
    folderStats.(measure).stats.span(3,:)=std(folderStats.(measure).span,0);
end